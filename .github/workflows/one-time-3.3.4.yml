name: One-time DevMate 3.3.4 hardening

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  harden:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install
        run: npm ci

      - name: Syntax-check hardening script
        run: node --check scripts/one-time-3.3.4.cjs

      - name: Apply final hardening
        run: node scripts/one-time-3.3.4.cjs

      - name: Synchronize 3.3.4 version
        shell: pwsh
        run: |
          npm version 3.3.4 --no-git-tag-version
          npm run version:sync

      - name: Remove one-time machinery before validation
        shell: pwsh
        run: |
          Remove-Item scripts/one-time-3.3.4.cjs -Force
          Remove-Item .github/workflows/one-time-3.3.4.yml -Force

      - name: Repository contracts
        run: npm run check

      - name: Full unit and policy suite
        run: npm run test:unit

      - name: Gateway smoke
        run: npm run smoke:gateway

      - name: Package VSIX
        run: npm run package:vsix

      - name: Packaged VSIX runtime smoke
        run: node scripts/smoke-vsix-runtime.mjs

      - name: Packaged VSIX shared tunnel smoke
        run: node scripts/smoke-vsix-shared-tunnel.mjs

      - name: Build Obsidian
        run: npm run build:obsidian

      - name: Obsidian runtime smoke
        run: node scripts/smoke-obsidian-runtime.mjs

      - name: Commit verified DevMate 3.3.4
        shell: pwsh
        run: |
          Remove-Item *.vsix -Force -ErrorAction SilentlyContinue
          git config user.name "kukutx"
          git config user.email "61358650+Kukutx@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Release DevMate 3.3.4 final tunnel hardening"
          git push origin HEAD:master
