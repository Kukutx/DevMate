'use strict';
const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const test=require('node:test');const settings=require('../vscode-host/tunnel-settings.js');const root=path.resolve(__dirname,'..');
test('rejects unknown connection providers',()=>{assert.equal(settings.tunnelProvider('ngrok'),'ngrok');assert.equal(settings.tunnelProvider('cloudflare-managed'),'cloudflare-managed');assert.throws(()=>settings.tunnelProvider('automatic'),/Unknown connection provider/)});
test('rejects coerced tunnel restart limits',()=>{assert.equal(settings.tunnelMaxRestarts(undefined),10);assert.equal(settings.tunnelMaxRestarts(0),0);assert.equal(settings.tunnelMaxRestarts(100),100);for(const value of ['10',null,-1,101,1.5,Number.NaN])assert.throws(()=>settings.tunnelMaxRestarts(value),/must be an integer/)});
test('shared instance and effective runtime keep strict provider validation without a mode parser',()=>{const platform=fs.readFileSync(path.join(root,'extension-entry-platform.js'),'utf8');const effective=fs.readFileSync(path.join(root,'vscode-host/effective-tunnel-settings.js'),'utf8');const schema=fs.readFileSync(path.join(root,'shared/instance-config.cjs'),'utf8');assert.doesNotMatch(platform,/setting\('deploymentMode'|setting\('tunnelProvider'/);assert.match(effective,/validateTunnelProvider/);assert.match(effective,/normalizeInstanceConfig/);assert.doesNotMatch(effective,/validateDeploymentMode|deploymentMode/);assert.match(schema,/CONNECTION_PROVIDERS/);assert.match(schema,/Unknown \$\{label\}/);assert.equal(fs.existsSync(path.join(root,'vscode-host/shared-deployment-config.js')),false)});
