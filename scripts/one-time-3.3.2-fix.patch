diff --git a/vscode-host/tunnel-controller.js b/vscode-host/tunnel-controller.js
--- a/vscode-host/tunnel-controller.js
+++ b/vscode-host/tunnel-controller.js
@@ -196,6 +196,7 @@ class TunnelController {
     this.ownershipCleanup = null;
     this.providerOutput = new WeakMap();
     this.providerSecrets = new WeakMap();
+    this.providerClosed = new WeakMap();
     this.stopping = false;
     this.disposed = false;
   }
@@ -358,6 +359,12 @@ class TunnelController {
     return this.providerSecrets.get(child) || [];
   }
 
+  async waitForProviderOutput(child, timeoutMs = 250) {
+    const closed = this.providerClosed.get(child);
+    if (!closed) return;
+    await Promise.race([closed, delay(timeoutMs)]);
+  }
+
   async providerReadyUrl(launch, match, child, timeoutMs) {
     if (launch.provider === 'external') return launch.publicUrl;
     const startedAt = Date.now();
@@ -367,7 +374,8 @@ class TunnelController {
     while (Date.now() <= deadline) {
       const output = this.childOutput(child);
       if (!childActive(child)) {
-        throw providerStartupError(match.provider, output, child, { secrets: this.childSecrets(child) });
+        await this.waitForProviderOutput(child);
+        throw providerStartupError(match.provider, this.childOutput(child), child, { secrets: this.childSecrets(child) });
       }
       if (launch.provider === 'cloudflare-quick') {
         discovered = parseTryCloudflareUrl(output) || discovered;
@@ -398,15 +406,27 @@ class TunnelController {
     this.childReady = false;
     this.providerOutput.set(child, '');
     this.providerSecrets.set(child, sensitiveValues.filter(Boolean));
+    let resolveClosed;
+    this.providerClosed.set(child, new Promise(resolve => { resolveClosed = resolve; }));
     const capture = chunk => {
       const previous = this.providerOutput.get(child) || '';
-      this.providerOutput.set(child, `${previous}${String(chunk)}`.slice(-MAX_PROVIDER_RESPONSE_BYTES));
+      const safeChunk = safeProviderOutput(match.provider, chunk, this.childSecrets(child));
+      this.providerOutput.set(child, `${previous}${safeChunk}`.slice(-MAX_PROVIDER_RESPONSE_BYTES));
+    };
+    const attachStream = stream => {
+      if (!stream) return;
+      if (typeof stream.read === 'function') {
+        let buffered;
+        while ((buffered = stream.read()) !== null) capture(buffered);
+      }
+      stream.on?.('data', capture);
     };
-    child.stdout?.on('data', capture);
-    child.stderr?.on('data', capture);
+    attachStream(child.stdout);
+    attachStream(child.stderr);
 
     let terminal = false;
     const finish = (code, signal) => {
+      resolveClosed?.();
       if (terminal) return;
       terminal = true;
       if (this.child !== child) return;
@@ -430,7 +450,6 @@ class TunnelController {
       const detail = outputTail(safeProviderOutput(match.provider, error.message || error, this.childSecrets(child)));
       this.logger(`Tunnel process error${detail ? `: ${detail}` : ''}`);
     });
-    child.once?.('exit', finish);
     child.once?.('close', finish);
   }
 
diff --git a/tests/tunnel-runtime-safety.test.cjs b/tests/tunnel-runtime-safety.test.cjs
--- a/tests/tunnel-runtime-safety.test.cjs
+++ b/tests/tunnel-runtime-safety.test.cjs
@@ -568,6 +568,7 @@ test('ngrok machine mode preserves NGROK_AUTHTOKEN and surfaces a redacted authe
     });
     assert.equal(spawnedToken, token);
     assert.equal(logs.some(line => line.includes(token)), false);
+    assert.doesNotMatch(controller.childOutput(child), new RegExp(token));
   } finally {
     if (previous === undefined) delete process.env.NGROK_AUTHTOKEN;
     else process.env.NGROK_AUTHTOKEN = previous;

diff --git a/tests/vscode-public-mcp-contract.test.cjs b/tests/vscode-public-mcp-contract.test.cjs
--- a/tests/vscode-public-mcp-contract.test.cjs
+++ b/tests/vscode-public-mcp-contract.test.cjs
@@ -8,7 +8,7 @@ const extension = fs.readFileSync(path.resolve(__dirname, '..', 'extension.js'),
 
 test('VS Code Start remains Gateway -> tunnel -> current-generation MCP verification -> Ready', () => {
-  const start = extension.indexOf('async function quickStart(ctx)');
+  const start = extension.indexOf('async function quickStart(ctx');
   const end = extension.indexOf('async function stopAll()', start);
   assert.ok(start >= 0 && end > start);
   const block = extension.slice(start, end);
@@ -66,7 +66,7 @@ test('VS Code Copy URL verifies and commits current generation before clipboard 
 });
 
 test('clipboard convenience cannot turn a verified Start into a failed lifecycle', () => {
-  const start = extension.indexOf('async function quickStart(ctx)');
+  const start = extension.indexOf('async function quickStart(ctx');
   const end = extension.indexOf('async function stopAll()', start);
   const block = extension.slice(start, end);
   const verify = block.indexOf('await verifyCurrentTunnel(publicUrl, tunnel.record, ctx)');
